import { SlashCommandBuilder } from "discord.js";

const slikaCommand = new SlashCommandBuilder()
  .setName("slika")
  .setDescription("NALUKNI SE V MOJ OBITELJSKI FOTOALBUM");

export default slikaCommand;
